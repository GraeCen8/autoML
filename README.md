# autoML
a custom ml framework that can take in a dataset or other data and a task definition and use it to produce a work model with simple deployment through a outputed function.  also will include a CLI for building models easier which can be called in a script 
